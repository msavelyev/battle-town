{
  "dependencies": {
    "express": "^4.17.1",
    "http-proxy-middleware": "^1.0.5",
    "parcel-bundler": "^1.12.4"
  },
  "scripts": {
    "clean": "rm -rf .cache dist",
    "start": "npm run clean && parcel ../web/public/index.html",
    "build": "npm run clean && parcel build ../web/public/index.html --no-source-maps"
  }
}
